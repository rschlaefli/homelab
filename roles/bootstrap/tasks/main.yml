# - name: Allow roland to sudo
# lineinfile:
# dest: /etc/sudoers
# state: present
# regexp: "^roland"
# line: "roland ALL=(ALL) ALL"
# validate: "visudo -cf %s"

- name: Disable swap
  sysctl:
    name: vm.swappiness
    value: "0"
